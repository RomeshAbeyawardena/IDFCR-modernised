<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="CopyProtosToProject" BeforeTargets="PrepareForBuild;BeforeBuild">
    <ItemGroup>
      <NuGetSourceFiles Include="$(MSBuildThisFileDirectory)..\contentFiles\any\any\Proto\Shared\idfcr-shared\**\*" />

      <P2PSourceFiles Include="$(MSBuildThisFileDirectory)Proto\Shared\idfcr-shared\**\*" />

      <AllSourceFiles Condition="'@(NuGetSourceFiles)' != ''" Include="@(NuGetSourceFiles)" />
      <AllSourceFiles Condition="'@(NuGetSourceFiles)' == ''" Include="@(P2PSourceFiles)" />
    </ItemGroup>

    <MakeDir Directories="$(ProjectDir)Proto\Shared\idfcr-shared" Condition="!Exists('$(ProjectDir)Proto\Shared\idfcr-shared')" />

    <Copy
      SourceFiles="@(AllSourceFiles)"
      DestinationFolder="$(ProjectDir)Proto\Shared\idfcr-shared\%(RecursiveDir)"
      SkipUnchangedFiles="true"
      OverwriteReadOnlyFiles="true" />

    <Message Text="[Idfcr] Synced protos to $(ProjectDir)Proto\Shared\idfcr-shared" Importance="high" />
  </Target>
</Project>